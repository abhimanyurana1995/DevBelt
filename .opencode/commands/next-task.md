You must complete ALL of the following steps in a single response. Do not stop between steps. Do not ask for confirmation. Do not report findings and wait — act immediately.

**Step 1:** Read `TASKS.md`. Find the first task marked `[ ] ready`. Note its number, name, and spec file path.

**Step 2:** Read the spec file for that task (path is listed next to the task in TASKS.md).

**Step 3:** Write the implementation file to the exact path listed in the spec under "File:". Use the registration pattern and HTML template from the spec exactly. Do not skip any functions listed in the spec.

**Step 4:** After the file is written successfully, update `TASKS.md` — change that task's `[ ] ready` to `[x] done`.

**Step 5:** Report: "Done: [task name] → [file path]"

If the write tool fails, retry once with the exact same content. If it fails again, report the error.

Do not stop after Step 1 or Step 2. Reading is never the final action. You must write the file.
